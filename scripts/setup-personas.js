// Retired: this script previously created shared-password identities and must
// never be used against a real Supabase project.
console.error('setup-personas.js is retired because shared test credentials are unsafe.');
console.error('Create isolated test identities through verified-email invitations instead.');
process.exitCode = 1;
